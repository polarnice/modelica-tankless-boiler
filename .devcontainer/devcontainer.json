{
  "name": "Modelica Tankless Boiler",
  "image": "mcr.microsoft.com/devcontainers/base:ubuntu",
  
  "features": {
    "ghcr.io/devcontainers/features/python:1": {
      "version": "3.11"
    }
  },
  
  "postCreateCommand": "bash -c 'curl -LsSf https://astral.sh/uv/install.sh | sh && curl -sSfL https://raw.githubusercontent.com/trufflesecurity/trufflehog/main/scripts/install.sh | sh -s -- -b $HOME/.local/bin && export PATH=\"$HOME/.local/bin:$PATH\" && cd /home/vscode/repos/modelica-tankless-boiler && make setup'",
  
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python",
        "ms-python.vscode-pylance"
      ],
      "settings": {
        "python.defaultInterpreterPath": "${workspaceFolder}/.venv/bin/python"
      }
    }
  },
  
  "remoteUser": "vscode"
}

